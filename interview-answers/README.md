面接回答の下書き置き場です。
「結論→根拠→具体例→学び」の順で簡潔に書くようにしてください。

## JS
### JSの組み込みクラス
**想定質問**
- インスタンスを作るときは `new` を使うが、配列とオブジェクト時は `new`を省略することができる。基本的には省略型を使用する。
- `Set`: 重複しない値を保存するコレクション。
- `Map`: キーと値のペアを保存するデータ構造。
- `Date`: 日時を扱うクラス。
- `Array`: 配列を扱うクラス。
- `Object`: オブジェクトを作るクラス。
- `Promise`: 非同期処理を扱うクラス。
