"データ型毎に扱えるメソッド"

"Map専用メソッド

const map = new Map()

使えるもの
・map.set(""a"",1): キーと値のペアを追加する
・map.get(""a""): 指定したキーの値を取得する
・map.has(""a""): 指定したキーが
存在するか確認する（true / false を返す）
・map.delete(""a""): 指定したキーと
値のペアを削除する
・map.clear(): Mapのすべてのキーと
値を削除する"